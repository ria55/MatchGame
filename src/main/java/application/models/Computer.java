package application.models;

public interface Computer {

    // abstract method
    // that means that the method has no body
    // interfaces' methods are PUBLIC by default
    int takeMatches(int matchesRemained);

}
